import { environment } from '../../../../environments/environment';

export const WORKOUT_URL = `${environment.serverUrl}/workout`;
export const EXERCISE_URL = `${environment.serverUrl}/exercise`;
export const EXERCISE_SET_URL = `${environment.serverUrl}/set`;
export const EXERCISE_TYPE_URL = `${environment.serverUrl}/exercise-type`;